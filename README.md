This is a repo to test cursor agent:

Write a small local web app that show the three-body problem, simulate this. Give parameters like init postions, simulation dt and etc that can be set in the web app 
